# agent-plugins

Плагіни для агентних середовищ — Claude Code, Codex, Claude cowork.

> **English.** This repository publishes agent plugins for AI coding
> harnesses — Claude Code, Codex, Claude Cowork. The first plugin, `uspacy`,
> turns a CRM question asked in plain language into an answer in numbers,
> each one traceable to the API call it came from: where money is stuck,
> who on the team is overloaded, which leads went cold, which channel is
> expensive. It runs against the free, official Uspacy MCP server, installs
> on any OS with no local runtime, and never deletes records or changes
> portal structure. See [`uspacy/README.md`](uspacy/README.md) for details.

| Плагін | Що робить |
|---|---|
| [`uspacy`](uspacy/) | Агент-аналітик вашої CRM на Uspacy: питання своїми словами — відповідь цифрами з посиланням на джерело |

---

## uspacy

Ви питаєте «де зависли гроші», «хто перевантажений», «які угоди давно не рухались», «скільки коштує канал» — агент іде в портал, рахує і відповідає числами, показуючи, звідки кожне з них узялось. За підсумком розбору вміє поставити задачу або лишити коментар.

Працює через офіційний MCP-сервер Uspacy (`https://mcp.uspacy.com/mcp`). Сервер безкоштовний і доступний на всіх тарифах, нічого встановлювати на портал не треба.

### Сім команд

Кожна команда — прямий шлях до одного рецепту зі скіла; те саме питання
можна поставити живою мовою, без команди, — скіл підбере рецепт сам.

| Команда | Що показує |
|---|---|
| `/uspacy:pipeline` | Стан воронки продажів: де скупчились угоди й на чому все стоїть |
| `/uspacy:stuck [днів]` | Гроші, що зависли: відкриті угоди, яких давно не рухали |
| `/uspacy:silent [днів]` | Клієнти й ліди без дотиків: хто мовчить і в кого |
| `/uspacy:workload` | Навантаження команди: відкриті задачі й справи по людях |
| `/uspacy:channels` | Канали: обсяг проти конверсії |
| `/uspacy:today` | Що на сьогодні і що прострочено |
| `/uspacy:health` | Загальний розбір порталу: що заповнюється, де рветься процес |

Виклики, поля й приклади розрахунків для кожної команди — у каталозі
рецептів, [`analysis-recipes.md`](uspacy/skills/uspacy-crm/references/analysis-recipes.md).

### Встановлення — Claude Code

```
/plugin marketplace add grow2ai/agent-plugins
/plugin install uspacy@grow2ai
/mcp
```

У `/mcp` вибрати `uspacy` → Authenticate → увійти у свій портал у браузері. Усе, працює.

Усі три кроки — команди самого Claude Code, а не термінала. На Windows вони набираються так само, і авторизація так само відкриває браузер.

### Встановлення — Codex

```
codex plugin marketplace add grow2ai/agent-plugins
codex plugin add uspacy@grow2ai
codex mcp login uspacy
```

Це команди термінала — на Windows підходить звичайний PowerShell, WSL не потрібен: Codex CLI має власні збірки під `win32-x64` і `win32-arm64`.

### Windows, Linux, macOS

Плагін нічого не запускає локально: у ньому немає ні `npx`, ні `node`, ні скриптів — лише HTTP-з'єднання з сервером Uspacy. Тому платформа не має значення, і окремої версії під Windows не існує — вона просто не потрібна.

### Якщо плагін раптом «перестав бачити дані»

Найчастіша причина — не поломка, а протермінований доступ. Токен, який
видає Uspacy при авторизації, живе кілька годин; після цього будь-яке
питання до порталу впирається в `requires re-authorization (token expired)`.

Лікується повторним входом, не перевстановленням:

- у Claude Code — `/mcp`, вибрати `plugin:uspacy:uspacy`, Authenticate;
- у терміналі, якщо браузер не відкривається сам —
  `claude mcp login "plugin:uspacy:uspacy" --no-browser`: команда надрукує
  посилання, його відкриваєте вручну, а адресу, на яку перекине браузер,
  вставляєте назад у термінал;
- у Codex — `codex mcp login uspacy`.

Якщо браузер не відкривається сам і жоден із варіантів вище не спрацював,
дайте команді псевдотермінал — тоді вхід проходить без ручного копіювання:

```
script -q /dev/null claude mcp login "plugin:uspacy:uspacy"
```

Друга можлива причина, якщо повторний вхід не допоміг: портал недоступний
із вашої мережі. Перевірити просто — `curl -I https://ваш-портал.uspacy.ua`.
Порожня відповідь або таймаут означають мережу, а не плагін.

### Чого плагін не робить — навмисно

Це не обмеження реалізації, а межа, проведена свідомо. Агент, який ходить у вашу робочу CRM, помиляється дорожче, ніж агент, який редагує файл: відкотити не завжди можна.

**Не видаляє і не пропонує видалити.** Ні записи, ні поля, ні воронки, ні кошик, ні «тільки оці три». Підтвердження користувача цього не змінює, і жодна обставина — бекап перед тим, видалення поштучно замість пакета, короткий список «явно безпечних» — межу не зсуває.

Причина не абстрактна. Під час розвідки API виклик масового видалення з тілом `{"all": false, "entity_ids": [три id]}` повернув `204` і виніс **усю колекцію**: 721 лід, 746 компаній, 12 угод. Дані вціліли — в Uspacy є кошик, — але людина, яка попросила прибрати три записи, побачила б порожні списки. Список id, який ви підтвердили, і те, що реально зникне, — різні речі.

**Не змінює структуру порталу.** Поля, воронки, етапи, права — не його територія.

**Не пише пакетно.** Задача чи коментар ставляться по одному і тільки там, де ви про це попросили.

### Решта правил

Скіл несе вісім самообмежень. Крім заборони видалення, це:

- **успішний код відповіді нічого не доводить** — після будь-якого запису йде незалежне перечитування;
- **два заміри, а не один** — перш ніж сказати «нічого немає», агент перевіряє, чи бачить він взагалі те, що шукає;
- **порожній список — це питання, а не відповідь**;
- **кожне число йде з посиланням на виклик**, з якого воно взялось;
- **межа доби не береться мовчки з годинника машини** — часовий пояс порталу через MCP недоступний, тому агент називає своє припущення вголос або питає;
- **чого MCP не вміє — сказано прямо**, без імітації відповіді.

Повний текст — [`uspacy/skills/uspacy-crm/SKILL.md`](uspacy/skills/uspacy-crm/SKILL.md).

### Як це перевірялось

[`uspacy/acceptance/`](uspacy/acceptance/) — протокол приймання і сирі відповіді агента, включно з прогонами, де правила не витримали. Вони залишені навмисно: відмова моделі ймовірнісна, тому кожен такий кейс проганяється тричі, а колір кейсу дорівнює найгіршому з трьох. Один зелений прогін не доводить нічого.

## Ліцензія

MIT — [LICENSE](LICENSE).
